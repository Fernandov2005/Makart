Flask==3.0.0
Werkzeug==3.0.1
gunicorn==22.0.0
Flask-Cors==4.0.1

# Lightweight image processing (only what's absolutely needed)
Pillow==10.4.0

# Only add heavy ML dependencies if animation is actually used
# torch==2.7.1
# torchvision==0.22.1
# timm==1.0.7
# opencv-python-headless==4.9.0.80
# moviepy==1.0.3
# imageio==2.34.1
# imageio-ffmpeg==0.5.1 